#spring.application.name=WeatherServer
#serverTimezone=UTC&
server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:keystore.p12
    key-store-password: 123456
    key-store-type: PKCS12
    key-alias: myserver
spring:
  application:
    name: WeatherServer
  datasource:
    url: jdbc:mysql://localhost:3306/weatherdata_db?useSSL=false&characterEncoding=utf8
    username: root
    password: (h/n)Nmdv7-d
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true